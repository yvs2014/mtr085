
Basic Build
------------------

It can be built with autotools or cmake

1) autotools
autoreconf -fi
./configure
make
#sudo make install

2) cmake
cmake -B _build -S $(pwd)
cmake --build _build
#sudo cmake --install _build --strip


Packaging
------------------

Template samples are located in 'package-templates/'

